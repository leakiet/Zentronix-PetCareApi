spring.application.name=TheGreenKitchen
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
app.tokenSecret=${JWT_SECRET}
app.tokenExpirationMs=${JWT_EXPIRATION_MS}

# Spring AI Configuration
spring.ai.openai.api-key=${OPENAI_API_KEY}
spring.ai.openai.chat.model=${OPENAI_MODEL}

# Email Configuration
spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# # Gmail SMTP Performance Optimization
# spring.mail.properties.mail.smtp.connectiontimeout=3000
# spring.mail.properties.mail.smtp.timeout=3000
# spring.mail.properties.mail.smtp.writetimeout=3000
# spring.mail.properties.mail.smtp.ssl.enable=false
# spring.mail.properties.mail.smtp.starttls.required=true
# spring.mail.properties.mail.smtp.connectionpoolsize=4
# spring.mail.properties.mail.smtp.connectionpooltimeout=10000

# Frontend URL
app.frontend.url=${FRONTEND_URL}
